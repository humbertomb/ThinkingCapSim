local ball = chaos.getLpo(0)
local net1 = chaos.getLpo(1)
local net2 = chaos.getLpo(2)

if ball.rho < 800 then
	if (net1.anchored > 0.5) and (math.abs(net1.theta) > 1.6) then 
		return true
	end
	if (net2.anchored > 0.5) and (math.abs(net2.theta) < 1.6) then 
		return true
	end
end

return false