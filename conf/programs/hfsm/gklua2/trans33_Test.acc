#ifdef TRACE_GOALKEEPER
if (CONT % TIMER == 0) 
    printf("Transition Center-IsNotFacing ang=%f\n",lps->Net1->getTheta());
#endif
return (lps->Net1->getAnchored() > ANCHORED_HIGH && lps->Net1->getTheta() > ANG_HIGH)
        || fabs(gs->getPosition()->theta - deg90) > ANG_HIGH;
// OR see net1 or my angle is PI/2 (facing net1)