/* * (c) 2000-2001 Humberto Martinez */package devices.pos;public class Paths extends Object {	// Current robot paths	protected Path					odom;				// Odometry calculated path	protected Path					corr;				// Corrected calculated path		protected Path					real;				// Real path (only when in simulation)	protected Position				offset;				// Current position correction			/* Constructors */	private Paths ()	{	}		public Paths (int pn)	{		odom	= new Path (pn);		corr	= new Path (pn);		real	= new Path (pn);				offset	= new Position ();	}		/* Accessor methods */	public final Path	 		odometry () 				{ return odom; }	public final Path	 		corrected () 				{ return corr; }	public final Path	 		real () 					{ return real; }		public final Position	 	correction () 				{ return offset; }		// Instance methods	public void correction (double x, double y, double a)	{		offset.set (x, y, a);	}			public void correction (Position offset)	{		this.offset.set (offset);	}	}